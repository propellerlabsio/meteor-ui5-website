<mvc:View xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="meteor-ui5-website.demo.2-filter.some-documents.SomeOrders">

    <!-- List some orders in table -->
    <Table id="OrdersTable">
        <!-- Info tool bar for displaying active filter criteria -->
        <infoToolbar>
            <Toolbar id="OrdersFilterBar"
                active="true"
                visible="true">
                <Label id="OrdersFilterLabel"
                    text="?" />
            </Toolbar>
        </infoToolbar>

        <!-- Columns -->
        <columns>
            <core:Fragment fragmentName="meteor-ui5-website.demo.1-binding.OrdersTableColumns"
                type="XML" />
        </columns>

        <!-- Item contents -->
        <core:Fragment fragmentName="meteor-ui5-website.demo.1-binding.OrdersTableItem"
            type="XML" />
    </Table>


</mvc:View>
