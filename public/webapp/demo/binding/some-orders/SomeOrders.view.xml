<mvc:View xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="meteor-ui5.demo.binding.some-orders.SomeOrders">

    <!-- List some orders in table -->
    <Table id="OrdersTable">
        <!-- Info tool bar for displaying active filter criteria -->
        <infoToolbar>
            <Toolbar id="OrdersFilterBar"
                active="true"
                visible="true">
                <Label id="OrdersFilterLabel"
                    text="?" />
            </Toolbar>
        </infoToolbar>

        <!-- Columns -->
        <columns>
            <core:Fragment fragmentName="meteor-ui5.demo.binding.OrdersTableColumns"
                type="XML" />
        </columns>

        <!-- Item contents -->
        <core:Fragment fragmentName="meteor-ui5.demo.binding.OrdersTableItem"
            type="XML" />
    </Table>


</mvc:View>
