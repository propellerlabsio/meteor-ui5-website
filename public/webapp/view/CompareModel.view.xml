<mvc:View
    	xmlns:mvc="sap.ui.core.mvc"
      xmlns:meteor-ui5-demo="meteor-ui5-demo.control"
      xmlns="sap.m"
      controllerName="meteor-ui5-demo.controller.CompareModel">

		<Page id="compareModel" title="{viewState>/pageTitle}" class="sapUiStdPage">
 		  <content>
        <meteor-ui5-demo:CompareModelControl
          id="modelExampleControl"
          demoViewName="{viewState>/demoViewName}"
          sourceFileView="{viewState>/sourceFileView}"
          sourceFileController="{viewState>/sourceFileController}"
          sourceFileTable="{viewState>/sourceFileTable}"
          />
			</content>
		</Page>

</mvc:View>
