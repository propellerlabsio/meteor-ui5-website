<mvc:View xmlns:core="sap.ui.core"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    controllerName="webapp.demo.filter.some-orders.SomeOrders">

    <!-- List some orders in table -->
    <Table id="OrdersTable">
        <!-- Info tool bar for displaying active filter criteria -->
        <infoToolbar>
            <Toolbar id="OrdersFilterBar"
                active="true"
                visible="true">
                <Label id="OrdersFilterLabel"
                    text="?" />
            </Toolbar>
        </infoToolbar>

        <!-- Columns -->
        <columns>
            <core:Fragment fragmentName="webapp.demo.binding.OrdersTableColumns"
                type="XML" />
        </columns>

        <!-- Item contents -->
        <core:Fragment fragmentName="webapp.demo.binding.OrdersTableItem"
            type="XML" />
    </Table>


</mvc:View>
