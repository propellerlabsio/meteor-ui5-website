<mvc:View xmlns:mvc="sap.ui.core.mvc"
    xmlns:webapp="webapp.control"
    xmlns="sap.m"
    controllerName="webapp.controller.Demo">

  <Page id="compareModel"
      binding="{viewState>/Demo}"
      title="{viewState>title}"
      class="sapUiStdPage">
    <content>

      <webapp:DemoViewer groupId="{viewState>groupId}"
          demoId="{viewState>_id}"
          infoFile="{viewState>infoFile}"
          demoViewName="{viewState>demoViewName}" />
    </content>
  </Page>
</mvc:View>
